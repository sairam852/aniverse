package com.example.aniverse.config

/**
 * App-level configuration / feature flags.
 */
object AppConfig {
    // Feature flags
    var showImages: Boolean = true
    var enableSync: Boolean = true
}

